#include <iostream>
#include <math.h>
using namespace std;

int computeTransFunc(int P[], int E[], int m, int e){
    int k;
    for (int q = 0; q <= m; q++){
        for (int i = 0; i < e; i++){
            k = min(m + 1, q + 2);
            while 
        }

    }
}